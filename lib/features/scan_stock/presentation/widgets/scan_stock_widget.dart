import 'package:flutter/material.dart';

class ScanStockWidget extends StatelessWidget {
  const ScanStockWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(title: const Text('ScanStock Widget'),),
      body: const Placeholder(),
    );
  }
}